#include <stdio.h>
#include "main.h"

/**
 * main - prints the name of the program
 * @argc: number of arguments
 * @argv: array of arguments
 *
 * Return: Always 0 (success)
 */
int main(int argc ـــattributeـــ((unused)), char *argv[])
{
	printf("%s\n", *array);

	return (0);
}
